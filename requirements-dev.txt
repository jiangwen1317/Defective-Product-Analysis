# 仓库级开发/检查依赖（三个子项目共享）
# 安装命令：.venv\Scripts\python.exe -m pip install -r requirements-dev.txt

pytest>=7.0.0    # 单元测试框架（check.bat 步骤 3-6）
ruff>=0.16.0,<0.17.0    # 静态检查，规则映射见根级 pyproject.toml（check.bat 步骤 1）；
                        # 锁定 0.16.x：违规处数快照与 ruff 判定行为强耦合，避免小版本升级导致门禁误报
# check.bat 步骤 2（存量基线豁免只减不增断言）由 tools/check_exemption_baseline.py 实现，仅用标准库，无额外依赖
